
/**
 * API Host configuration for backend communication.
 * When testing on an Android Emulator, localhost points to the emulator itself.
 * 10.0.2.2 acts as a bridge to the host machine's localhost.
 */
export const API_BASE_URL = 'http://192.168.0.42:8080';
